---
import type { SocialNetwork } from '@data/types';

type Props = {
    socialNetwork: SocialNetwork
}

const { socialNetwork } = Astro.props;
---
<a 
    class="bg-grey-700 p-4 rounded-md font-semibold hover:bg-secondary-100 hover:text-grey-800" 
    href={socialNetwork.url}
    target="_blank"
>
    {socialNetwork.name}
</a>